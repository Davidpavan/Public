apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zelar.fullname" . }}-test-connection"
  labels:
    {{- include "zelar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zelar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
